#pragma once
#include <string>
#include <vector>
#include <queue>

static class PriorityQueueEval{

public:
    static double evaluate(std::vector<std::string>& tokens);
}

static double PriorityQueueEval::evaluate(std::vector<std::string>& tokens){
    return 0;
    }